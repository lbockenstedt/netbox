venv/
node_modules/
.DS_Store
__pycache__/
*.pyc

# Secrets and credentials — never commit these
.env
*.key
*.pem
*.p12
*.pfx
keys.json
hub_secret.json
secrets.json

# Local git worktrees — never part of the repo
.claude/worktrees/
.wt-*/

# mTLS material and runtime secrets issued by the hub — never commit these
certs/
.env.lock
*.crt
